#!/bin/bash

# Pre-install script for Roon AI

set -e

# Stop existing service if running
launchctl stop com.221b.roon-ai 2>/dev/null || true
launchctl unload /Library/LaunchDaemons/com.221b.roon-ai.plist 2>/dev/null || true

exit 0

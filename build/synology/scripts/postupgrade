#!/bin/bash
# Post-upgrade script
# Runs after upgrade completes

PACKAGE_NAME="roon-ai"
PACKAGE_DIR="/var/packages/${PACKAGE_NAME}"
TARGET_DIR="${PACKAGE_DIR}/target"

# Make binary executable
chmod +x "${TARGET_DIR}/roon-ai"

echo "Roon AI upgraded successfully"

exit 0
